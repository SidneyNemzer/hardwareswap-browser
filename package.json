{
  "name": "hardwareswap-browser",
  "version": "0.1.0",
  "private": true,
  "homepage": "/hardwareswap-browser",
  "dependencies": {
    "material-ui": "^0.18.1",
    "react": "^15.5.4",
    "react-dom": "^15.5.4",
    "react-tap-event-plugin": "^2.0.1"
  },
  "devDependencies": {
    "react-scripts": "1.0.7"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "rm ./docs -r && react-scripts build && mv ./build ./docs",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
